import React from 'react';
import landing from '../images/landing.png'

console.log(landing);

const Landing = () => {
  return (
    <div>
      <img src={landing}/>
    </div>
  );
};

export default Landing;